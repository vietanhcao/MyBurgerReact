import React from 'react'


import './Toolbar.css'

const Toolbar = (props) => {
  return (<header className='toolbar'>
    <div>Menu</div>
    <div>Logo</div>
    <nav>
      ...
    </nav>
  </header>)
}

export default Toolbar